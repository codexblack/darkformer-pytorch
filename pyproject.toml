[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "darkformer-pytorch"
dynamic = ["version"]
description = "Data-aware random feature attention for PyTorch"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Shreyan Mohanty" }]
keywords = [
  "attention",
  "darkformer",
  "linear-attention",
  "performer",
  "pytorch",
  "transformers",
]
license = "MIT"
license-files = ["LICENSE"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "Typing :: Typed",
]
dependencies = [
  "torch>=2.4",
]

[project.optional-dependencies]
benchmark = [
  "performer-pytorch==1.1.4",
]
dev = [
  "build>=1.2",
  "mypy>=1.10",
  "numpy>=1.26,<2",
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.6",
  "twine>=5.1",
]

[project.urls]
Homepage = "https://github.com/codexblack/darkformer-pytorch"
Issues = "https://github.com/codexblack/darkformer-pytorch/issues"
Paper = "https://arxiv.org/abs/2603.04127"
Repository = "https://github.com/codexblack/darkformer-pytorch"

[tool.hatch.version]
path = "src/darkformer_pytorch/__init__.py"

[tool.hatch.build.targets.sdist]
include = [
  "/src",
  "/tests",
  "/benchmarks",
  "/README.md",
  "/CHANGELOG.md",
  "/LICENSE",
  "/photo.png",
  "/pyproject.toml",
]

[tool.hatch.build.targets.wheel]
packages = ["src/darkformer_pytorch"]

[tool.ruff]
line-length = 88
target-version = "py310"
extend-exclude = ["tmp"]

[tool.ruff.lint]
select = [
  "B",
  "C4",
  "D",
  "E",
  "F",
  "I",
  "N",
  "PERF",
  "PIE",
  "RUF",
  "SIM",
  "UP",
]
ignore = [
  "D100",
  "D104",
  "D105",
  "D107",
]

[tool.ruff.lint.isort]
known-first-party = ["darkformer_pytorch"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.pycodestyle]
max-doc-length = 88

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D", "S101"]

[tool.ruff.format]
docstring-code-format = true
line-ending = "lf"

[tool.pytest.ini_options]
minversion = "8.0"
addopts = [
  "--strict-config",
  "--strict-markers",
  "--cov=darkformer_pytorch",
  "--cov-report=term-missing:skip-covered",
]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["darkformer_pytorch"]

[tool.coverage.report]
exclude_also = [
  "if TYPE_CHECKING:",
  "raise NotImplementedError",
]
show_missing = true

[tool.mypy]
python_version = "3.10"
files = ["src", "tests", "benchmarks"]
strict = true
warn_return_any = false
warn_unreachable = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = ["flash_attn"]
ignore_missing_imports = true
